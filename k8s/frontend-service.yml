apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80       # Exposed port inside cluster
      targetPort: 80 # Container port
  type: LoadBalancer
